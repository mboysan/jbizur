package protocol.commands.bizur;

import protocol.commands.NetworkCommand;

public class LeaderResponse_NC extends NetworkCommand {
    @Override
    public String toString() {
        return "LeaderResponse_NC{} " + super.toString();
    }
}
