package protocol.commands.bizur;

import protocol.commands.NetworkCommand;

public class AckVote_NC extends NetworkCommand {
    @Override
    public String toString() {
        return "AckVote_NC{} " + super.toString();
    }
}
