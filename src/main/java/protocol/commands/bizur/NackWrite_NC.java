package protocol.commands.bizur;

import protocol.commands.NetworkCommand;

public class NackWrite_NC extends NetworkCommand{
    @Override
    public String toString() {
        return "NackWrite_NC{} " + super.toString();
    }
}
