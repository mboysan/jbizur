package protocol.commands.bizur;

import protocol.commands.NetworkCommand;

public class NackVote_NC extends NetworkCommand {
    @Override
    public String toString() {
        return "NackVote_NC{} " + super.toString();
    }
}
