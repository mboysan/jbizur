package protocol.commands.common;

import protocol.commands.NetworkCommand;

public class Nack_NC extends NetworkCommand {
    @Override
    public String toString() {
        return "Nack_NC{} " + super.toString();
    }
}
