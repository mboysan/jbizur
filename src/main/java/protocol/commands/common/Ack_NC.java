package protocol.commands.common;

import protocol.commands.NetworkCommand;

public class Ack_NC extends NetworkCommand {
    @Override
    public String toString() {
        return "Ack_NC{} " + super.toString();
    }
}
