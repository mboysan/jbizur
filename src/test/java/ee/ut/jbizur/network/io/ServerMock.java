package ee.ut.jbizur.network.io;

import ee.ut.jbizur.network.address.Address;

public class ServerMock extends AbstractServer {
    public ServerMock(String name, Address serverAddress) {
        super(name, serverAddress);
    }
}
